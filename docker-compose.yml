services:

  # ─────────────────────────────────────────────
  #  0. MONGODB  (local, replaces Atlas)
  # ─────────────────────────────────────────────
  mongodb:
    image: mongo:7.0
    container_name: hasibha-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"          # expose for optional local tools (Compass etc.)
    volumes:
      - mongo_data:/data/db    # persistent storage — data survives restarts
    networks:
      - hasibha-net
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # ─────────────────────────────────────────────
  #  1. AUTH BACKEND  (Node.js · port 3210)
  # ─────────────────────────────────────────────
  auth-backend:
    build:
      context: ./auth-backend
      dockerfile: Dockerfile
    image: hasibha/auth-backend:latest
    container_name: hasibha-auth
    restart: unless-stopped
    ports:
      - "3210:3210"
    env_file:
      - ./auth-backend/.env
    environment:
      - NODE_ENV=production
      - MONGO_URI=mongodb://mongodb:27017/hexaverse-auth
    networks:
      - hasibha-net
    depends_on:
      mongodb:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3210/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # ─────────────────────────────────────────────
  #  2. HOME BACKEND  (Node.js · port 5001)
  # ─────────────────────────────────────────────
  home-backend:
    build:
      context: ./home-backend
      dockerfile: Dockerfile
    image: hasibha/home-backend:latest
    container_name: hasibha-home
    restart: unless-stopped
    ports:
      - "5001:5001"
    env_file:
      - ./home-backend/.env
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongodb:27017/hexaverse-auth
      # Override AI_AGENT_URL to use the Docker service name instead of localhost
      - AI_AGENT_URL=http://ai-service:8000
      # Firebase JSON is mounted from the project root
      - FIREBASE_SERVICE_ACCOUNT_PATH=/app/firebase-adminsdk.json
    volumes:
      # Mount the Firebase service-account key into the container
      - ./hasibha-notificatio-firebase-adminsdk-fbsvc-ac5586ab12.json:/app/firebase-adminsdk.json:ro
    networks:
      - hasibha-net
    depends_on:
      mongodb:
        condition: service_healthy
      ai-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # ─────────────────────────────────────────────
  #  3. AI SERVICE  (Python/FastAPI · port 8000)
  # ─────────────────────────────────────────────
  ai-service:
    build:
      context: ./grad_project_ai
      dockerfile: Dockerfile
    image: hasibha/ai-service:latest
    container_name: hasibha-ai
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ./grad_project_ai/.env
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/hexaverse-auth
      - HOME_BACKEND_URL=http://home-backend:5001
    networks:
      - hasibha-net
    depends_on:
      mongodb:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/docs').raise_for_status()"]
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 40s

# ─────────────────────────────────────────────
#  Shared network
# ─────────────────────────────────────────────
networks:
  hasibha-net:
    driver: bridge

# ─────────────────────────────────────────────
#  Named volumes (data persists across restarts)
# ─────────────────────────────────────────────
volumes:
  mongo_data:
